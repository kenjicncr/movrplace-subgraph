type Bucket @entity {
  id: ID!

  position: Int!

  pixels: [Int!]!

  lastBlockUpdated: Int!
}

type Account @entity {
  id: ID!

  totalPixelsPlaced: Int!

  address: Bytes!
}

type Charity @entity {
  id: ID!

  name: String!

  totalPixels: Int!
}